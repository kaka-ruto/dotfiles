# Create a symlink of this file to the location yamllint expects to find it
# mac: ln -sf /Users/name/config/yamllint/config /Users/name/.condig/yamllint/config
# This file extends the default conf by adjusting some options.
---
extends: default

rules:
  # 120 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 120
    level: warning
